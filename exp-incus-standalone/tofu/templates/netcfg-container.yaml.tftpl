network:
    version: 2
    ethernets:
        eth0:
            dhcp4: false
            dhcp6: false
            addresses: [ ${ip_address}/24 ]
            routes:
              - to: default
                via: ${gateway}
            nameservers:
              search: [ ${join(", ", dns_search)} ]
              addresses: [ ${dns} ]
