# OpenTofu

```bash
tofu --version
tofu init
tofu validate
tofu fmt
tofu plan
```

# Prerequisites for calling Tofu

Load Cloudflare API key
```bash
source ~/bin/load_cloudflare_api_token.sh
source ~/bin/load_powerdns_api_token.sh
```
